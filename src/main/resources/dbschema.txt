create table `oder_dtls` (`order_id` integer not null, 
`order_date` datetime(6), `address` varchar(255), 
`order_name` varchar(255), primary key (`order_id`))

{
    "name":"bramaranbha",
    "email":"bramaaramba@gmail.com",
    "password":"Sha123",

   "roles":"ROLE_ADMIN"
}